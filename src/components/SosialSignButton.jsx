export default function SsoSign({children}) {
    return (
        <button className={'flex items-center gap-5 w-full justify-center py-4 shadow rounded'}>
            {children}
        </button>
    )
}